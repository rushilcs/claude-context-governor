# Test Project Rules (for M0 spike validation)

- Always use spaces for indentation (2 spaces). Never use tabs.
- Use TypeScript for all source files.
- All API endpoints must follow the /v1/resource/:id pattern.
